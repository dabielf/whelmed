# Domain Docs

Before exploring this repo, read:

- `CONTEXT.md`, if it exists.
- Relevant files in `docs/adr/`, if they exist.

Missing files are fine. Continue silently.

## Layout

This is a single-context repo:

/
├── CONTEXT.md
├── docs/adr/
└── src/

Use the words defined in `CONTEXT.md`.

If work conflicts with an ADR, say so clearly.
